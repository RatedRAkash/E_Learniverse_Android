package com.example.e_learniverse_android.kotlin_code.model

// in Kotlin Class we define "Constructor" during the "Declaration" of the Class
class ContactModel(var img: Int,
                   var name: String,
                   var number: Int)